manifest {
    homePage = 'https://github.com/lindaxiang/dna-seq-processing-tools'
    description = 'Sequencing reads processing tools'
    nextflowVersion = '>=20.10'
}

docker {
    enabled = true
    runOptions = '-u \$(id -u):\$(id -g)'
}
